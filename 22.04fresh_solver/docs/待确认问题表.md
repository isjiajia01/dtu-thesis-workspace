# 待确认问题表

下面这些问题不影响当前工作区与代码骨架落地，但会影响后续实现是否按你的真实 thesis 口径精确收口。

| 编号 | 问题 | 当前默认处理 | 为什么需要你确认 |
|---|---|---|---|
| Q1 | benchmark JSON 的真实字段名是否已经最终固定？ | 当前 `io/benchmarks.py` 做了宽松兼容解析 | 如果字段名、嵌套层级或 metadata 键名和实际不一致，后续需要精确收口 |
| Q2 | `DeliveryEarliestTime / DeliveryLatestTime` 的最终口径是否是约束 `service_start`？ | 当前文档按 `service_start` 口径写 | 这会直接影响 routing feasibility 计算 |
| Q3 | `PickingTaskTime` 在你 thesis 主线里是否明确作为 warehouse-resource 约束启用？ | 当前架构里保留为 repair / diagnostics 输入 | 如果你想把它作为主约束，需要更早进入 depot model |
| Q4 | `Gates` 是否统一按 15 分钟 bucket 建模，并且所有实例都一样？ | 当前 repair baseline 按 15 分钟处理 | 如果某些实例 bucket 粒度不同，repair 逻辑要改 |
| Q5 | 每台车“最多两趟”是否是所有 benchmark 都固定成立？ | 当前代码骨架默认 `max_trips_per_day = 2` | 如果存在仓/车型差异，需要实例化到 vehicle config |
| Q6 | 当前 thesis 是否需要把 `Loading/Unloading` 都精确进 trip relay？ | 当前文档已纳入，代码骨架尚未精算 | 这会影响 trip2 feasibility 实现顺序 |
| Q7 | `Colli / Volume / Weight` 是否就是你论文主线的最终车辆容量口径？ | 当前按多维容量处理，不引入完整 3D packing | 如果你后面真要做箱体级装载，代码架构要提前留扩展口 |
| Q8 | East / West 是否和 Herlev 一样都要纳入主实验链路？ | 当前工作区已放入全部 benchmark 与 matrices | 如果主文只写 Herlev + West，实验目录和脚本可以先聚焦 |
| Q9 | 你希望第一版代码主语言就定为 Python 吗？ | 当前我已经用 Python 搭了可运行骨架 | 如果你计划后续转 Julia 主后端，接口层要更早解耦 |
| Q10 | 你是否希望我下一步直接把 baseline 从“骨架可运行”推进到“真实可行解构造器”第一版？ | 当前已到可以直接开始实现代码的状态 | 这是最自然的下一步，我可以继续往下写 |

## 当前已经默认定下来的主线
- thesis 主架构：`controller + daily routing backend + depot-aware repair`
- 不把 giant MIP 作为主线
- 不把 pure RL 作为主线
- 不把完整 3D packing 作为当前主线
- 当前第一版代码骨架以 Python 落地
