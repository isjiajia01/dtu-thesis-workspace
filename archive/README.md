# Archive

This folder holds non-active artifacts kept only for reference.

## Current layout

- `crash-dumps/` — local diagnostic dumps that should not be treated as thesis source material.

Prefer moving stale root-level clutter here instead of leaving it mixed with active workspace files.
